{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm --prefix frontend install && npm --prefix frontend run build && rm -rf public && mkdir -p public && cp -r frontend/dist/. public/",
  "functions": {
    "api.py": {
      "excludeFiles": "{frontend/**,test_*.py,train_*.py,audit_*.py,collect_*.py,diagnostic_*.py,debug_*.py,export_*.py}"
    }
  }
}
